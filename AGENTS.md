# 🤖 Vangcur — AI Agent Coding Guidelines & System Integrity Rules
**সর্বশেষ অডিট তারিখ:** সেপ্টেম্বর ২০২৬  
**প্রযোজ্য:** Claude, ChatGPT, Cursor, Windsurf, GitHub Copilot এবং যেকোনো AI/হিউম্যান ডেভেলপার।

---

## 📌 ১. মিশন ও টেকনোলজি স্ট্যাক (Mission & Tech Stack)

Vangcur একটি মিশন-ক্রিটিক্যাল ই-কমার্স প্ল্যাটফর্ম। এই রিপোজিটরিতে কোড লেখা বা এডিট করার সময় প্ল্যাটফর্মের বিদ্যমান ব্যবসায়িক লজিক, সিকিউরিটি গার্ড এবং ইউআই কনসিস্টেন্সি যাতে কোনো অবস্থাতেই নষ্ট না হয় — তা নিশ্চিত করা এই গাইডের মূল উদ্দেশ্য।

- **ফ্রেমওয়ার্ক:** Next.js 15 (App Router, Server Actions, `after()` background tasks)
- **কোর লাইব্রেরি:** React 19, TypeScript (Strict Mode)
- **অ্যানিমেশন:** Motion (v13) / Framer Motion
- **স্টাইলিং:** Tailwind CSS v3 + কাস্টম ডিজাইন টোকেনস
- **স্টেট ম্যানেজমেন্ট:** Zustand v5 (Persisted Stores: Cart, Wishlist, Auth, Language)
- **ডাটাবেজ ও অথ:** Supabase PostgreSQL (RLS, SSR Cookies, Postgres RPCs)
- **মিডিয়া ডেলিভারি:** Cloudinary (WebP on-the-fly resizing + Client Canvas Compression)

---

## 🛑 ২. "কখনো ভাঙা যাবে না" — অতি-সংবেদনশীল কোর ফাইলসমূহ (The Sacred Rules)

নিচের ফাইলগুলোতে হাত দেওয়ার সময় সর্বোচ্চ সতর্কতা অবলম্বন করতে হবে:

### ১. `app/actions/checkout.ts` (জিরো-ট্রাস্ট অর্ডার ইঞ্জিন)
- **নিয়ম:** ক্লায়েন্ট থেকে পাঠানো কোনো পণ্যের মূল্য (`price`) বা অগ্রিম পেমেন্টের হিসাব সার্ভার কখনো বিশ্বাস করবে না।
- সার্ভার অ্যাকশনে অবশ্যই ডাটাবেজ থেকে আসল পণ্যের দাম, কুপন ডিসকাউন্ট ও শিপিং চার্জ যাচাই করে সার্ভারেই মোট বিল এবং ৫% অগ্রিম + ১.৫% বিকাশ ফি হিসাব করতে হবে।
- অর্ডার ইনসার্টের আগে অবশ্যই `decrement_product_stock` RPC দিয়ে অটোমিক স্টক যাচাই ও কমাতে হবে।
- টেলিগ্রাম নোটিফিকেশন ও কুপন ব্যবহার কাউন্টার আপডেট অবশ্যই Next.js 15-এর `after()` ব্লকের ভেতরে ব্যাকগ্রাউন্ডে চালাতে হবে।

### ২. `lib/checkoutData.ts` (৩-টায়ার ডায়নামিক পেমেন্ট ও ২০k লিমিট)
- **টায়ার ১ (< ৳৮,০০০):** ফিক্সড **৳২০০** অগ্রিম।
- **টায়ার ২ (৳৮,০০০ – ৳২০,০০০):** মোট বিলের **৫% মূল অগ্রিম + ১.৫% বিকাশ ট্রানজেকশন ফি** (`calculateAdvancePayment`).
- **টায়ার ৩ (> ৳২০,০০০):** অনলাইন চেকআউট স্থায়ীভাবে ব্লক। সরাসরি `OPEN_BULK_ORDER_EVENT` ডিসপ্যাচ করে WhatsApp মডাল খুলতে হবে। `MAX_ONLINE_ORDER_TOTAL = 20000` ধ্রুবক কখনো পরিবর্তন করা যাবে না।

### ৩. `lib/useHistoryModal.ts` (পপস্টেট হিস্ট্রি ও `suppressHistoryCleanup`)
- **নিয়ম:** যখনই কোনো মডাল বা ড্রয়ার বন্ধ করার (`onClose()` / `setOpen(false)`) ঠিক পরপরই `router.push()` দিয়ে নেভিগেট করা হবে, তখন সেই `router.push()`-এর ঠিক আগে অবশ্যই `suppressHistoryCleanup()` কল করতে হবে।
- তা না হলে মডাল বন্ধের বিলম্বিত `window.history.back()` নতুন নেভিগেশনকে বাতিল করে দেবে।

### ৪. `app/layout.tsx` (ডিজিট-অনলি ইউনিকোড-রেঞ্জ ফন্ট লিংক)
- `<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Noto+Sans+Bengali:wght@400;500;600;700&text=...&display=swap" />` লিংকটি কখনো মুছবেন না বা পরিবর্তন করবেন না।
- এটি শুধুমাত্র বাংলা সংখ্যার (`০-৯`) ইউনিকোড-রেঞ্জ ওভাররাইড করে সংখ্যাকে স্পষ্ট ও সুন্দরভাবে রেন্ডার করে।

### ৫. `lib/i18n/useT.ts` (অসীম লুপ প্রতিরোধে `useCallback`)
- `useT()` হুকের রিটার্ন করা `t` ফাংশনটি অবশ্যই `useCallback((text) => translate(text, lang), [lang])` দিয়ে মেমোইজড থাকতে হবে। সাধারণ অ-মেমোইজড ফাংশন দিলে `useEffect([t])` থাকা কম্পোনেন্টগুলোতে অসীম রি-রেন্ডার লুপ ঘটবে।

### ৬. `lib/floatButtonsData.ts` ও `lib/productData.ts` (রিয়েল-টাইম ইউনিক চ্যানেল)
- সুপাবেস রিয়েল-টাইম চ্যানেল তৈরি করার সময় অবশ্যই ইউনিক নাম (`${name}-${Math.random().toString(36).slice(2, 9)}`) ব্যবহার করতে হবে। ফিক্সড স্ট্রিং দিলে ডুপ্লিকেট সাবস্ক্রিপশন ক্র্যাশ ঘটবে।

---

## 👥 ৩. রোল ও পারমিশন নিয়মাবলী (Roles & Privileges)

- **মেইন এডমিন (মালিক):** সম্পূর্ণ ডাটাবেজ ও ব্যাকএন্ড নিয়ন্ত্রণ।
- **অথরাইজড মডারেটর (`mehedivibecoding@gmail.com`):**
  - কাস্টমার রিভিউতে বাইপাস সুবিধা (সরাসরি ভেরিফায়েড ও অ্যাপ্রুভড রিভিউ পোস্ট)।
  - রিভিউ অ্যাপ্রুভ, রিজেক্ট ও ডিলিট করার ক্ষমতা।
  - কাস্টমার কিউএন্ডএ (Q&A)-তে অফিসিয়াল "Vangcur টিম" ব্যাজ সহ উত্তর দেওয়ার ক্ষমতা।
  - চেকআউটে ২০,০০০ টাকার সীমা ও রেট লিমিট বাইপাস সুবিধা।
  - *সীমাবদ্ধতা:* ব্যাকএন্ডের মূল ডাটাবেজ সার্ভিস রোল এক্সেস পাবে না।

---

## 🎨 ৪. UI/UX ও ডিজাইন সিস্টেম নির্দেশিকা (Design Rules for AI)

1. **ট্রাই-কালার ক্যানভাস:** প্রতিটি মডাল, পপআপ ও নতুন পেজে ব্যাকগ্রাউন্ড হবে:  
   `bg-gradient-to-b from-brand-bg/35 via-[#DCEBFD]/45 to-white`
2. **কালার স্ট্যান্ডার্ড:**
   - **প্রধান সিগনেচার স্কাই-ব্লু:** `#44A7FC` (`brand-light`, hover `#3C93DE`) — বাটন, লিংক, আইকন, বর্ডার ও হাইলাইটে এটাই ব্যবহার করতে হবে।
   - **ডিপ ব্লু:** `#0058C7` (`brand-primary`) — শুধুমাত্র লোগো এবং ডিপ কন্ট্রাস্ট টাচের জন্য। আইকন বা সাধারণ বাটনে এটি ব্যবহার করবেন না।
3. **🚫 নো-সেরিফ রুল:** কোনো হেডিং বা টাইটেলে `font-display` (Playfair Display) ব্যবহার করা যাবে না। সব হেডিং হবে `font-body font-bold text-ink`।
4. **🚫 নো-ইমোজি পলিসি:** ইউআইতে কাঁচা ইমোজি ব্যবহার নিষিদ্ধ। সব জায়গায় প্রফেশনাল হ্যান্ড-ড্রন লাইন SVG আইকন ব্যবহার করুন।  
   *(ব্যতিক্রম: শুধুমাত্র `OrderCard`-এর মেটা লাইনে অনুমোদিত 📅 ও 👤 ইমোজি)*।
5. **প্রাইস রেন্ডারিং:** মূল্য সবসময় ইংরেজি ডিজিটে `৳{price.toLocaleString('en-US')}` ফরম্যাটে রেন্ডার হবে। পিস লেবেল বাংলায় **"৩ পিছ"** / ইংরেজিতে **"3 Pcs"**।
6. **প্রাইমারি বাটন স্ট্যান্ডার্ড:**
   ```tsx
   className="shimmer-sheen w-full rounded-full bg-gradient-to-r from-info to-brand-light py-[13.5px] font-body text-[15px] font-bold text-white shadow-sh2 transition-[filter] duration-brand hover:brightness-[1.03] active:scale-95 disabled:opacity-60"
   ```

---

## ⚠️ ৫. কোড ডেলিভারি ও ফরম্যাটিং প্রোটোকল (STRICT)

1. **Zero Truncation:** কোনো কোড ফাইলে `// ... rest of code` বা কোনো ফাংশন বাদ দেওয়া সম্পূর্ণ নিষিদ্ধ। শুরু থেকে শেষ পর্যন্ত সম্পূর্ণ ফাইল দিতে হবে।
2. **একক কোড ব্লক নিয়ম:** প্রতিটি ফাইলের সম্পূর্ণ কন্টেন্ট অবশ্যই **একটামাত্র অবিচ্ছিন্ন কোড ব্লকের ভেতরে** দিতে হবে। ফাইলের নাম ও পাথ কোড ব্লকের বাইরে আলাদা লাইনে লিখতে হবে।
3. **টাইপস্ক্রিপ্ট টাইটনেস:** কোড লেখার পর নিশ্চিত করুন যাতে কোনো ডুপ্লিকেট অবজেক্ট কী (TS1117), টাইপ মিসম্যাচ বা অব্যবহৃত টাইপ এরর না থাকে।
4. **কোনো হ্যালুসিনেটেড প্যাকেজ নয়:** প্রজেক্টে বিদ্যমান প্যাকেজ (`package.json`) ছাড়া নতুন কোনো থার্ড-পার্টি লাইব্রেরি মালিকের স্পষ্ট অনুমতি ছাড়া যোগ করা যাবে না।
