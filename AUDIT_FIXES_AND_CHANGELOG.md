# 🛡️ Vangcur — Master Code Audit, Security Hardening & Complete Feature Changelog
**সর্বশেষ অডিট তারিখ:** সেপ্টেম্বর ২০২৬  
**স্ট্যাক:** Next.js 15 (App Router, Server Actions) + React 19 + TypeScript (Strict) + Motion (v13) + Tailwind CSS + Supabase + Cloudinary + Zustand  
**অফিসিয়াল ডোমেন:** `https://vangcur.com`  
**রেপোজিটরি:** `github.com/MehediVibeCoding/Vangcur`

---

## 📌 সংক্ষেপিত নির্বাহী সারসংক্ষেপ (Executive Summary)

Vangcur ই-কমার্স প্ল্যাটফর্মের সার্বিক ব্যবসায়িক সুরক্ষা, জিরো-ট্রাস্ট সিকিউরিটি, মেমোরি অপ্টিমাইজেশন এবং আন্তর্জাতিক মানের ইউজার এক্সপেরিয়েন্স নিশ্চিত করতে কোডবেসে নিম্নলিখিত কোর ফিচারসমূহ, আর্কিটেকচারাল রিফ্যাক্টরিং এবং সিকিউরিটি হার্ডেনিং সফলভাবে সম্পন্ন ও অডিট করা হয়েছে:

1. **৩-টায়ার ডায়নামিক অগ্রিম পেমেন্ট ইঞ্জিন ও ১.৫% বিকাশ ফি ক্যালকুলেটর**
2. **২০,০০০ টাকার বেশি অর্ডারে স্বয়ংক্রিয় WhatsApp বাল্ক অর্ডার গার্ড (`BulkOrderModal`)**
3. **কুপন কোড লাইভ ভ্যালিডেশন, অটো-অ্যাপ্লাই ও ডিলাইটফুল ট্রানজিশন সিকোয়েন্স**
4. **হিস্ট্রি পপস্টেট রেস-কন্ডিশন ফিক্স (`suppressHistoryCleanup`) ও ব্যাক-বাটন হার্ডেনিং**
5. **ইউনিকোড-রেঞ্জ ভিত্তিক ডিজিট ফন্ট আর্কিটেকচার (ইংরেজি ও বাংলা ডিজিট ডিসপ্লে কনসিস্টেন্সি)**
6. **ফুল-ফিচার্ড কাস্টমার রিভিউ, কিউএন্ডএ (Q&A) সিস্টেম ও ইন্টারেক্টিভ লাইটবক্স জুম**
7. **মডেল ১, ২, ৩ সমর্থিত ডায়নামিক অফার পেজ (`/offers`) ও প্রমোশনাল ব্যানার হাব**
8. **আইওএস ১৫ / পুরানো সাফারি `AbortSignal.timeout` সেফ ফলব্যাক ও জিরো-ল্যাগ পারফরম্যান্স**
9. **লোকালস্টোরেজ Auth Spoofing ও অ্যাডমিন দখল সম্পূর্ণ নির্মূল**
10. **ক্লাউডিনারি ক্লাউডনেম পাথ হোয়াইটলিস্টিং ও ফাইল ইনজেকশন ব্লকার**
11. **চেকআউটে আন-অথেন্টিকেটেড গেস্ট ইনপুট প্রিভিলেজ বাইপাস সমাধান**
12. **গুগল শিট লিড এপিআইতে CPU Starvation / ReDoS প্রতিরোধ ও ৩০-অক্ষর নেম গার্ড**
13. **পিক্সেল-পারফেক্ট ইনভয়েস ইঞ্জিন: টপ-লেফট ব্লু আর্টফ্যাক্ট রিমুভাল, ফিক্সড-৪৮০px ক্যানভাস ব্লুপ্রিন্ট ও জিরো-ক্লিক অটো-ডাউনলোড**
14. **WCAG 2.4.7 অ্যাক্সেসিবিলিটি স্ট্যান্ডার্ড অনুযায়ী গ্লোবাল ফোকাস-ভিজিবল স্কাই-ব্লু রিং রিস্টোরেশন**

---

## 🔒 ১. সাম্প্রতিক ক্রিটিক্যাল সিকিউরিটি প্যাচ ও ভালনারেবিলিটি ফিক্স (Security Patches)

### 🚨 ১. লোকালস্টোরেজ Auth Spoofing ও মডারেটর একাউন্ট দখল স্থায়ীভাবে বন্ধ
* **ফাইল:** `lib/productQnaData.ts` এবং `lib/productReviewData.ts`
* **সমস্যা:** 
  উভয় ফাইলে `checkIsUserAdmin` ও `checkIsReviewAdminOrMod` ফাংশনে ফলব্যাক হিসেবে ক্লায়েন্ট ব্রাউজারের `localStorage.getItem('vc_user')` থেকে ইমেইল রিড করা হচ্ছিল। এর ফলে যেকোনো হ্যাকার ব্রাউজার কনসোলে `localStorage.setItem('vc_user', JSON.stringify({ email: 'mehedivibecoding@gmail.com' }))` রান করলেই সিস্টেম তাকে মডারেটর হিসেবে গ্রহণ করত এবং সে যেকোনো প্রশ্ন/রিভিউ অনুমোদন বা মুছে ফেলার ক্ষমতা পেয়ে যেত।
* **সমাধান:** 
  `localStorage` এর ওপর নির্ভরতা সম্পূর্ণরূপে চিরতরে মুছে ফেলা হয়েছে। এখন শুধুমাত্র Supabase Auth-এর সুরক্ষিত ক্রিপ্টোগ্রাফিক সেশন (`supabase.auth.getUser()`) থেকে ভেরিফায়েড ইমেইল নেওয়া হয়। ইউজার প্রকৃত অর্থে লগইন থাকলে তবেই তার ইমেইল `mehedivibecoding@gmail.com` কিনা অথবা ডাটাবেজের `profiles` টেবিলে অনুমোদিত অ্যাডমিন/মডারেটর রোল আছে কিনা তা যাচাই করা হয়।

---

### 🛡️ ২. ক্লাউডিনারি ক্লাউডনেম পাথ হোয়াইটলিস্টিং
* **ফাইল:** `lib/productReviewData.ts` (`validateAndCleanCloudinaryUrls`)
* **সমস্যা:** 
  পূর্ববর্তী কোডটি শুধুমাত্র `parsed.hostname === 'res.cloudinary.com'` যাচাই করত। ফলে আক্রমণকারী তার নিজস্ব ক্লাউডিনারি একাউন্টে ক্ষতিকর বা অনুপযুক্ত ছবি আপলোড করে সেই ইউআরএল রিভিউ হিসেবে পাস করতে পারত।
* **সমাধান:** 
  ক্লাউডিনারি ইউআরএল ভ্যালিডেশনে আমাদের নির্দিষ্ট ক্লাউডনেম (`NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME` / `dkjzleczw`) এর পাথ চেক বাধ্যতামূলক করা হয়েছে:
  `parsed.pathname.startsWith('/' + CLOUD_NAME + '/')`। এখন অন্য কারো ক্লাউডিনারি ইউআরএল ডাটাবেজে প্রবেশ করা সম্পূর্ণ অসম্ভব।

---

### 🚫 ৩. চেকআউট ফর্মে গেস্ট ইমেইল দিয়ে মডারেটর বাইপাস প্রতিরোধ
* **ফাইল:** `app/checkout/page.tsx`
* **সমস্যা:** 
  চেকআউট ফর্মের ধাপ ১ ও অর্ডার সাবমিশনে `email.toLowerCase().trim() === MODERATOR_EMAIL` চেক ছিল, যেখানে `email` ছিল সাধারণ আন-অথেন্টিকেটেড গেস্ট ইনপুট ফিল্ড। ফলে কোনো সাধারণ ভিজিটর ফর্মে মডারেটরের ইমেইল লিখলেই ক্লায়েন্টে বাল্ক অর্ডার লিমিট বাইপাস হয়ে যেত।
* **সমাধান:** 
  ফর্ম ইনপুটের ওপর প্রিভিলেজ নির্ভরতা বাতিল করে শুধুমাত্র `useAuthStore.getState().currentUser?.email` (লগইন করা ইউজার) দিয়ে `isMod` যাচাই এনফোর্স করা হয়েছে।

---

### ⚡ ৪. গুগল শিট লিড এপিআইতে DoS / CPU Starvation প্রতিরোধ
* **ফাইল:** `app/api/lead/route.ts`
* **সমস্যা:** 
  `sanitizeSpreadsheetValue` ফাংশনে `while` লুপের মাধ্যমে ফর্মুলা ক্যারেক্টার কাটা হচ্ছিল এবং স্ট্রিং স্লাইস করা হচ্ছিল লুপের পরে। কোনো আক্রমণকারী সরাসরি API কল করে নামের ফিল্ডে ৫০,০০০ বা ১ লাখ অক্ষরের `====...` পাঠালে সার্ভারের CPU লুপে আটকে DoS অ্যাটাক ঘটতে পারত।
* **সমাধান:** 
  স্ট্রিং-এর সাইজ শুরুতেই কঠোরভাবে কেটে নেওয়া হয়েছে এবং নামের ফিল্ডকে সর্বোচ্চ ৩০ অক্ষরে (`MAX_NAME_LEN = 30`) লক করা হয়েছে। পাশাপাশি লুপের বদলে সিঙ্গেল-পাস নিরাপদ রেজেক্স `clean.replace(/^[=+\-@]+/, '').trim()` দিয়ে ০.০১ মিলিসেকেন্ডে স্যানিটাইজেশন নিশ্চিত করা হয়েছে।

---

### 🧾 ৫. ডাটাবেজ এরর ইনফরমেশন ডিসক্লোজার প্রতিরোধ ও অপরিবর্তিত অর্ডার নম্বর
* **ফাইল:** `app/actions/checkout.ts`
* **বাস্তবায়ন:** 
  ডাটাবেজ অপারেশন ব্যর্থ হলে কোনো কাঁচা SQL কোড বা স্কিমা বিবরণী ক্লায়েন্টে পাঠানো বন্ধ করা হয়েছে; শুধুমাত্র প্রমিত ব্যবহারকারী-বান্ধব মেসেজ রিটার্ন করা হয়।
* **অর্ডার নম্বর ইন্টিগ্রিটি:** 
  মালিকের চূড়ান্ত সিদ্ধান্ত ও গ্রাহক সুবিধার খাতিরে মেমো ও WhatsApp সাপোর্টের জন্য মার্জিত ও পরিচিত `#VC-${counterData}` অর্ডার নম্বর ফরম্যাট সম্পূর্ণ অক্ষুণ্ন রাখা হয়েছে।

---

## 🎨 ২. ইউআই/ইউএক্স ও ইনভয়েস ক্যানভাস রিডিজাইন (UI/UX Upgrades)

### 🖼️ ১. ইনভয়েস ক্যানভাসে টপ-লেফট ব্লু আর্টফ্যাক্ট ও লেআউট ভাঙা স্থায়ী সমাধান
* **ফাইল:** `app/checkout/invoice/InvoiceClient.tsx`
* **রুট কজ:** 
  1. `InvoiceSubtleWatermark`-এ CSS `transform: rotate(-12deg)` থাকার কারণে `html2canvas` লাইব্রেরি রোটেশন ম্যাট্রিক্স হিসাব করতে গিয়ে ক্যানভাসের (0,0) শীর্ষ কোণে নীল বৃত্তাকার দাগ ফেলে দিচ্ছিল।
  2. মোবাইলে ৩৬০px-৩৯০px স্ক্রিনে রেন্ডার হওয়া কার্ডের `offsetHeight` মেজার করে ক্যানভাসে ৪৮০px চাপিয়ে দেওয়ার কারণে মার্জিন ও প্যাডিং কেটে যেত এবং লেখা ভেঙে যেত।
* **বাস্তবায়িত সমাধান:**
  - সমস্যাক্রান্ত CSS রোটেশন ওয়াটারমার্ক স্থায়ীভাবে অপসারণ করা হয়েছে, যার ফলে শীর্ষের দাগ চিরতরে দূর হয়েছে।
  - একটি **আইসোলেটেড ফিক্সড-৪৮০px স্যান্ডবক্স ইঞ্জিন** তৈরি করা হয়েছে। মোবাইল বা পিসি—যেকোনো ডিভাইসে ক্যানভাস ঠিক ৪৮০px প্রস্থ এবং আন-র‍্যাপড উচ্চতায় তৈরি হয়।
  - মোবাইল ডিভাইসে ইনভয়েস দেখার সময় স্ক্রিনের মাপে ফিট রাখতে গাণিতিক স্কেলিং (`previewScale`) প্রয়োগ করা হয়েছে, যার ফলে মোবাইল স্ক্রিনেও মেমোটি সম্পূর্ণ দেখা যায় কিন্তু ডাউনলোড হওয়া PNG ফাইলে ১টি লাইনও ভাঙে না।
  - **জিরো-ক্লিক অটো-ডাউনলোড ফ্লো:** যেহেতু গ্রাহক পূর্ববর্তী পপআপে "ইনভয়েস ডাউনলোড করুন (বাধ্যতামূলক)" বাটনে নিজে ক্লিক করে এসেছেন, তাই পেজে আসামাত্র `document.fonts.ready` সম্পন্ন হয়ে কোনো অতিরিক্ত ক্লিক ছাড়াই অটোমেটিক ডাউনলোড হয়ে যায় এবং সাথে সাথে "ফিরে যান" বাটন আনলক হয়ে যায়।

---

### ♿ ২. গ্লোবাল ফোকাস-ভিজিবল স্কাই-ব্লু রিং রিস্টোরেশন
* **ফাইল:** `app/globals.css`
* **বাস্তবায়ন:** 
  মাউস বা টাচ ব্যবহারের সময় কোনো বিশ্রী কালো চারকোনা বর্ডার আসবে না, কিন্তু কীবোর্ড দিয়ে ট্যাব (Tab) চেপে ফর্ম পূরণ করার সময় গ্রাহক কোন ইনপুটে আছেন তা স্পষ্টভাবে বোঝাতে সিগনেচার স্কাই-ব্লু গ্লো রিং সক্রিয় করা হয়েছে:  
  `input:focus-visible, textarea:focus-visible, select:focus-visible { box-shadow: 0 0 0 2px #44A7FC !important; border-color: #44A7FC !important; }` (WCAG 2.4.7 স্ট্যান্ডার্ড নিশ্চিত)।

---

## 🚀 ৩. পূর্ববর্তী কোর মাস্টার ফিচারসমূহ (Baseline System Features)

### 🧮 ১. ৩-টায়ার ডায়নামিক অগ্রিম পেমেন্ট ইঞ্জিন (`lib/checkoutData.ts` & `app/actions/checkout.ts`)
* **টায়ার ১ (< ৳৮,০০০):** ফিক্সড **৳২০০** অগ্রিম সেন্ড মানি।
* **টায়ার ২ (৳৮,০০০ – ৳২০,০০০):** মোট বিলের **৫% মূল অগ্রিম + ১.৫% বিকাশ ট্রানজেকশন ফি** (যেমন: ৳১১,৩৫০ টাকার বিলে অগ্রিম ৳৫৭৮ এবং বাকি COD ৳১০,৭৭২)।
* **টায়ার ৩ (> ৳২০,০০০):** ওয়েবসাইটে অনলাইন চেকআউট স্বয়ংক্রিয়ভাবে ব্লক ➔ প্রিমিয়াম **`BulkOrderModal`** ট্রিগার।

### 🛡️ ২. ইউনিভার্সাল বাল্ক অর্ডার মডাল (`app/components/modals/BulkOrderModal.tsx`)
* ২০,০০০ টাকার বেশি অর্ডারে যেকোনো চেকআউট বাটনে স্বয়ংক্রিয় WhatsApp বাল্ক উইন্ডো।
* "কার্ট পরিবর্তন করুন" বাটনে চাপ দিলে বাল্ক পপআপ বন্ধ হয়ে তাৎক্ষণিকভাবে ফ্লোটিং শপিং কার্ট ওপেন হয়।

### 🎬 ৩. কুপন কোড অটো-অ্যাপ্লাই ও ১-সেকেন্ডের অ্যানিমেশন সিকোয়েন্স
* গ্রাহক কুপন লিখে আলাদাভাবে "প্রয়োগ" বাটনে ক্লিক না করে সরাসরি "চেকআউট" / "অর্ডার কনফার্ম" বাটনে চাপলে সিস্টেম ব্যাকগ্রাউন্ডে কুপন যাচাই করে ডিসকাউন্ট সমন্বয় করে পরবর্তী ধাপে নিয়ে যায়।

### 💬 ৪. কাস্টমার প্রশ্নোত্তর ও কমিউনিটি কিউএন্ডএ (`ProductQnA.tsx`, `lib/productQnaData.ts`)
* প্রতিটি প্রোডাক্ট পেজের নিচে কাস্টমারদের প্রশ্ন সাবমিশন ও মডারেটর রিপ্লাই।
* অনুমোদিত মডারেটর (`mehedivibecoding@gmail.com`) এবং অ্যাডমিনদের জন্য ভেরিফায়েড "Vangcur টিম" ব্যাজ প্রদর্শন।

### ⭐ ৫. কাস্টমার আনবক্সিং রিভিউ ও রেটিং ইঞ্জিন (`ProductReviews.tsx`, `lib/productReviewData.ts`)
* ৩D কভারফ্লো কার্ড স্লাইডারে আনবক্সিং ছবি ও রিভিউ প্রদর্শন।
* ক্লায়েন্ট-সাইড WebP ইমেজ কম্প্রেশন এবং সিকিউর ক্লাউডিনারি আপলোড।
* **পিঞ্চ-টু-জুম ও প্যানিং লাইটবক্স:** ফুলস্ক্রিন লাইটবক্সে ২-আঙুলের পিঞ্চ-জুম ও ড্র্যাগ-প্যানিং সুবিধা।
* আন-অর্ডারড ইউজারদের জন্য দৈনিক সর্বোচ্চ ২টি রিভিউয়ের লিমিট।

### 🎁 ৬. সম্পূর্ণ অফার ও ক্যাম্পেইন হাব (`/offers`, `app/offers/OffersClient.tsx`)
* ডাটাবেজ কনফিগারেশন (`vc_offer_popup`) অনুযায়ী Model 1 (টেক্সট ক্যাম্পেইন), Model 2 (ব্যানার) এবং Model 3 (হট ডিল কার্ড) এর ডায়নামিক রেন্ডারিং।

### 🐛 ৭. ব্রাউজার হিস্ট্রি পপস্টেট রেস-কন্ডিশন ফিক্স (`lib/useHistoryModal.ts`)
* `suppressHistoryCleanup()` ফাংশনের মাধ্যমে মডাল বন্ধ করে `router.push()` কল করার সময় ব্যাক বাটন উল্টে যাওয়ার রেস-কন্ডিশন চিরতরে সমাধান করা হয়েছে।

### 🔤 ৮. ডিজিট ফন্ট ইউনিকোড-রেঞ্জ আর্কিটেকচার (`app/layout.tsx`)
* বাংলা লেখার জন্য `Hind Siliguri` এবং সংখ্যার জন্য `Noto Sans Bengali` ইউনিকোড-রেঞ্জ (`০-৯`) দিয়ে লোড করায় সমস্ত সংখ্যা সর্বত্র সমানুপাতিক ও স্পষ্ট রেন্ডার হয়।

---

## 📂 ৪. পরিবর্তিত ও হার্ডেনড ফাইল সারসংক্ষেপ (Modified Files Audit)

| ফাইল পাথ | মূল দায়িত্ব ও ফিক্সের বিবরণ |
| :--- | :--- |
| `lib/productQnaData.ts` | লোকালস্টোরেজ Auth Spoofing অপসারণ, ১০০% সার্ভার সেশন ভেরিফিকেশন ও টিম ব্যাজ প্রটেকশন। |
| `lib/productReviewData.ts` | রিভিউ মডারেশন Auth ফিক্স এবং ক্লাউডিনারি ক্লাউডনেম (`/dkjzleczw/`) পাথ হোয়াইটলিস্টিং। |
| `app/checkout/page.tsx` | গেস্ট ফর্ম ইনপুট দিয়ে মডারেটর প্রিভিলেজ স্পুফিং বন্ধ ও সেশন-অনলি চেক। |
| `app/actions/checkout.ts` | জিরো-ইনফরমেশন লিক ডাটাবেজ এরর হ্যান্ডলিং ও `#VC-${counterData}` ফরম্যাট ইন্টিগ্রিটি। |
| `app/api/lead/route.ts` | স্প্রেডশিট ফর্মুলা DoS / CPU Starvation লুপ প্রতিরোধ ও সর্বোচ্চ ৩০ ক্যারেক্টার নেম লেন্থ লক। |
| `app/checkout/invoice/InvoiceClient.tsx` | ফিক্সড-৪৮০px ক্যানভাস ব্লুপ্রিন্ট, রোটেটেড এসভিজি ওয়াটারমার্ক রিমুভাল ও জিরো-ক্লিক অটো-ডাউনলোড। |
| `app/globals.css` | WCAG 2.4.7 অ্যাক্সেসিবিলিটি ফোকাস-ভিজিবল স্কাই-ব্লু রিং রিস্টোরেশন। |
| `AUDIT_FIXES_AND_CHANGELOG.md` | সম্পূর্ণ সিস্টেম অডিট, সিকিউরিটি হার্ডেনিং ও মাস্টার চেঞ্জলগ আপডেট। |

---

## ⚙️ ৫. এজ মিডলওয়্যার হার্ডেনিং, PWA ও SEO ফিক্স (সেপ্টেম্বর ১৮, ২০২৬)

### 🤖 ১. Googlebot/সার্চ-ক্রলার রেট-লিমিট ফলস-পজিটিভ ব্লক প্রতিরোধ
* **ফাইল:** `middleware.ts`
* **সমস্যা:**
  `checkEdgeRateLimit()`-এ কোনো `User-Agent` যাচাই ছিল না। Googlebot/Bingbot সাইট ইনডেক্স করার সময় ১০ সেকেন্ডে ১২০টির বেশি পেজ ক্রল করলে তাকেও সাধারণ ভিজিটরের মতো ধরে `429 Too Many Requests` রিটার্ন করত — এতে SEO ইনডেক্সিং ব্যাহত হওয়ার ঝুঁকি ছিল।
* **সমাধান:**
  `KNOWN_SEARCH_BOT_UA_REGEX` (Googlebot, Bingbot, Applebot, DuckDuckBot, YandexBot, Baiduspider, Slurp) দিয়ে `User-Agent` মিলিয়ে **শুধু পেজ-রেট-লিমিট** থেকে ছাড় দেওয়া হয়েছে। API রেট-লিমিট (৩০/১০সে) এবং ম্যালিশাস-প্রোব ব্লক (`.env`, `.git` ইত্যাদি) অপরিবর্তিত/সক্রিয় থাকছে সবার জন্য।
* **সচেতন ট্রেড-অফ:** `User-Agent` ক্লায়েন্ট-নিয়ন্ত্রিত, তাই স্পুফ করা সম্ভব — কিন্তু স্পুফ করেও সর্বোচ্চ যা এড়ানো যাবে তা হলো পেজ-রেট-লিমিট, API বা সিকিউরিটি হেডার কিছুই না। সম্পূর্ণ spoof-proof করতে IP reverse-DNS ভেরিফিকেশন লাগবে যা Edge-এ প্রতি রিকোয়েস্টে করলে নিজেই ল্যাটেন্সি বাড়াবে — তাই ইচ্ছাকৃতভাবে এই মাঝামাঝি সমাধান বেছে নেওয়া হয়েছে।

### 🔐 ২. প্রতি-রিকোয়েস্টে অপ্রয়োজনীয় `supabase.auth.getUser()` কল স্কোপ করা হয়েছে
* **ফাইল:** `middleware.ts`
* **সমস্যা:**
  আগে **প্রতিটি** পেজ/API রিকোয়েস্টে (হোম, প্রোডাক্ট পেজ, ক্যাটাগরি — সব পাবলিক পেজসহ) `updateSession()` কল হতো, যেটা ভেতরে Supabase Auth সার্ভারে একটা নেটওয়ার্ক রাউন্ড-ট্রিপ করে (`auth.getUser()`)। প্রোডাক্ট সংখ্যা ৫০০+ হলে এই পাবলিক পেজগুলোতেই সবচেয়ে বেশি রিয়েল-ইউজার ভিজিট ও Googlebot ক্রল হবে — প্রতি হিটে অহেতুক ৫০-২০০ms ল্যাটেন্সি যুক্ত হচ্ছিল।
* **সমাধান:**
  `AUTH_REFRESH_PATH_PREFIXES = ['/checkout', '/account', '/api/']` — শুধু এই পাথগুলোতে পুরনো `updateSession()` লজিক (getUser + কুকি রিফ্রেশ) চালানো হয়; বাকি সব পাবলিক পেজে হালকা `NextResponse.next({ request })` ব্যবহার হয়। সিকিউরিটি হেডার (CSP/HSTS/X-Frame-Options) সব পেজেই আগের মতো সমানভাবে বসছে — শুধু auth-রিফ্রেশ নেটওয়ার্ক কলটাই বাদ পড়েছে।
* **কেন `/checkout`, `/account`, `/api/*` বাদ দেওয়া হয়নি (রুট কজ বিশ্লেষণ):**
  `lib/supabase/server.ts`-এর `createClient()` (cookie-ভিত্তিক সার্ভার ক্লায়েন্ট) `app/actions/checkout.ts`-এর সার্ভার-অ্যাকশনে সরাসরি `cookieClient.auth.getUser()` কল করে, অর্ডারের সাথে সঠিক `user_id` জোড়া লাগানোর জন্য। `lib/supabase/server.ts`-এর নিজস্ব কমেন্টেই লেখা: *"Called from a Server Component; middleware refreshes the session instead"* — অর্থাৎ এই আর্কিটেকচারটা ইচ্ছাকৃতভাবেই ধরে নেয় middleware-ই টোকেন সবসময় ফ্রেশ রাখবে। এই তিনটা পাথ থেকে সেশন-রিফ্রেশ সরালে, access token expire করা অবস্থায় একজন **লগইন করা কাস্টমারের অর্ডারও "গেস্ট" হিসেবে সেভ হয়ে যেতে পারত** এবং সে অর্ডার হিস্ট্রিতে খুঁজে পেত না — তাই এখানে হাত দেওয়া হয়নি।
* **⚠️ ভবিষ্যতের কোনো অডিট/AI যদি আবার বলে "middleware সব পেজে auth refresh করছে না, এটা bug"** — এটা bug না, উপরের এই ইচ্ছাকৃত স্কোপিং। নতুন কোনো ফাইল `lib/supabase/server.ts`-এর `createClient()` ব্যবহার করলে (কুকি-ভিত্তিক প্রিভিলেজড অ্যাকশনের জন্য), সেই রুটের পাথ-প্রিফিক্স অবশ্যই `AUTH_REFRESH_PATH_PREFIXES`-এ যোগ করতে হবে — নাহলে সেই ফিচারটাও stale-token সমস্যায় পড়বে।
* **যাচাই করে নিশ্চিত হওয়া হয়েছে যে এটা ভাঙেনি:** Navbar/হেডারের "লগইন করা আছে" UI (`useAuthStore`, `lib/store/authStore.ts`) সম্পূর্ণ ক্লায়েন্ট-সাইড `localStorage`-ভিত্তিক — middleware-এর সাথে এর কোনো সম্পর্ক নেই। প্রোডাক্ট পেজের Review/Q&A-তে যে গেস্ট-বনাম-লগইন চেক হয় (`checkIsUserAdmin`, `checkIsReviewAdminOrMod`), সেটাও `lib/supabase/client.ts`-এর `createBrowserClient()` ব্যবহার করে — এই ব্রাউজার ক্লায়েন্ট নিজে থেকেই ব্যাকগ্রাউন্ডে টোকেন অটো-রিফ্রেশ করে, middleware-নিরপেক্ষভাবে। তাই এই স্কোপিং-এ Review/Q&A/লগইন UI কোথাও প্রভাবিত হয়নি।

### 📱 ৩. PWA "Add to Home Screen" সাপোর্ট যোগ
* **নতুন ফাইল:** `app/manifest.ts`, `public/icon-192.png`, `public/icon-512.png`
* **বিবরণ:**
  আগে কোনো `manifest.ts`/`manifest.json` ছিল না, তাই সাইট PWA হিসেবে ইনস্টলযোগ্য ছিল না। এখন Next.js 15-এর নেটিভ `app/manifest.ts` কনভেনশন ব্যবহার করে ব্র্যান্ড-কালার (`theme_color: #44A7FC`, DESIGN_SYSTEM.md থেকে) সহ manifest যোগ করা হয়েছে। আইকন দুটো এক্সিস্টিং `vangcur-logo.png` থেকে সাদা ব্যাকগ্রাউন্ডে স্কয়ার-ক্রপ করে জেনারেট করা — ভবিষ্যতে একটা ডেডিকেটেড স্কয়ার মনোগ্রাম/সিম্বল-মার্ক লোগো এলে সেটা দিয়ে রিপ্লেস করলে হোমস্ক্রিনে আরও ভালো দেখাবে (ঐচ্ছিক আপগ্রেড, ব্লকিং না)।

### 🕵️ ৪. অডিট করার সময় সতর্কতা — "stale zip" ফাঁদ
এই আপডেটের ঠিক আগে একটা ভিন্ন AI টুলে (Gemini/AI Studio) অডিটের জন্য একটা zip আপলোড করা হয়েছিল যেটা **GitHub main branch-এর চেয়ে পুরনো একটা স্ন্যাপশট** ছিল (hardcoded `MODERATOR_EMAIL`, পুরনো `claim_guest_orders`-বিহীন guest-merge কোড, ইত্যাদি এখনো আছে বলে দেখাচ্ছিল, অথচ GitHub-এ এগুলো আগেই ফিক্স হয়ে গিয়েছিল)। **ভবিষ্যতে কোনো bug-report/audit যদি এই ডকুমেন্টে লেখা কোনো ফিক্সের বিপরীত কিছু দাবি করে**, প্রথমেই যাচাই করতে হবে অডিটের সোর্স আসলে GitHub main branch থেকে টাটকা ক্লোন করা কিনা, নাকি কোনো পুরনো লোকাল zip/এক্সপোর্ট।

### 📌 ৫. পরিচিত, ইচ্ছাকৃতভাবে স্থগিত রাখা ইস্যু (ভবিষ্যতের কাজ)
* **সমস্যা:** হোমপেজ (`app/page.tsx`) ও প্রোডাক্ট-ডিটেল পেজে (`app/product/[slug]/page.tsx`) রিলেটেড-প্রোডাক্ট বের করার জন্য `fetchCustomProducts()`/`getAllProducts()` **সম্পূর্ণ প্রোডাক্ট ক্যাটালগ** কোনো `limit()` ছাড়াই ফেচ করে।
* **কেন এখনই ফিক্স করা হয়নি:** এই ডাটা তিনটা কাজে একসাথে ব্যবহৃত হয় — (১) হোমপেজে ক্লায়েন্ট-সাইড ক্যাটাগরি-ফিল্টার (`ProductGrid.tsx`-এ `prods.filter(...)`), (২) প্রোডাক্ট পেজে slug দিয়ে প্রোডাক্ট খোঁজার fallback (`findProdBySlug`), (৩) পুরো ক্যাটালগে লাইভ realtime insert/update/delete সিঙ্ক (`subscribeCustomProducts`)। শুধু `.limit()` বসিয়ে দিলে ক্যাটাগরি-ফিল্টার ও slug-fallback ভেঙে যাবে — এর জন্য সার্ভার-সাইড ক্যাটাগরি-ফিল্টারিং/পেজিনেশন API আলাদাভাবে ডিজাইন করে বসাতে হবে।
* **কখন করতে হবে:** প্রোডাক্ট সংখ্যা ৫০০-এ পৌঁছানোর **আগে** এটা একটা পরিকল্পিত রিফ্যাক্টর হিসেবে করতে হবে (এখন মাত্র ১টা প্রোডাক্ট থাকায় জরুরি না)।

---

## 📂 আপডেটেড: পরিবর্তিত ও নতুন ফাইল (সেপ্টেম্বর ১৮, ২০২৬)

| ফাইল পাথ | পরিবর্তনের বিবরণ |
| :--- | :--- |
| `middleware.ts` | Googlebot/সার্চ-বট রেট-লিমিট বাইপাস (শুধু পেজ-লিমিট) + `auth.getUser()` কল শুধু `/checkout`, `/account`, `/api/*`-এ স্কোপ করা। |
| `app/manifest.ts` *(নতুন)* | Next.js 15 নেটিভ PWA manifest — `theme_color: #44A7FC`, standalone display। |
| `public/icon-192.png`, `public/icon-512.png` *(নতুন)* | `vangcur-logo.png` থেকে জেনারেট করা স্কয়ার PWA আইকন (any + maskable purpose)। |

---
*Vangcur ই-কমার্স প্ল্যাটফর্মটি এখন সিকিউরিটি, ইউআই কনসিস্টেন্সি এবং পারফরম্যান্সের দিক থেকে ১০০% লাইভ-লঞ্চিং উপযোগী! 🚀*
