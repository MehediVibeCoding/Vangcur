# 🎨 Vangcur — Design System & UI/UX Blueprint
**সর্বশেষ অডিট ও আপডেট:** সেপ্টেম্বর ২০২৬ (সম্পূর্ণ কোডবেস অডিট করে পুনর্লিখিত — শুধু বাস্তবে ব্যবহৃত টোকেন/প্যাটার্নই এখানে আছে)
**স্টাইল ফ্রেমওয়ার্ক:** Tailwind CSS v3 (`tailwind.config.ts`) + Custom CSS (`app/globals.css`) + Zustand (থিম/কার্ট/উইশলিস্ট স্টেট)

> এই ফাইলটা এখন সরাসরি রিপোর `tailwind.config.ts` ও `app/globals.css` অডিট করে লেখা — কোনো অনুমান বা পুরনো/অব্যবহৃত প্ল্যান এখানে রাখা হয়নি। যা কিছু এখানে আছে, সেটা আসলেই কোডে চালু আছে।

---

## 💎 ১. ব্র্যান্ড কালার আর্কিটেকচার (Brand Colors)

Vangcur-এর সিগনেচার কালার **স্কাই-ব্লু (`#44A7FC`)** — এবং এটাই প্যালেটের সবচেয়ে গাঢ় অনুমোদিত নীল টোন। এর চেয়ে গাঢ় বা vivid কোনো নীল (যেমন পুরনো `#0058C7` / `#005EFC` টোকেন, যেগুলো design-system-এ ভুলবশত ছিল এবং সরিয়ে ফেলা হয়েছে) কোনো UI-তে ব্যবহার করা যাবে না।

| টোকেন (Token) | হেক্স কোড | রোল ও বাস্তব ব্যবহার |
| :--- | :--- | :--- |
| **`brand-light`** | `#44A7FC` | প্রধান সিগনেচার কালার — বাটন, লিংক, অ্যাক্টিভ আইকন, ব্যাজ, ফ্লোটিং বাটন, বর্ডার। |
| **`brand-light-hover`** | `#3C93DE` | বাটন/লিংকের হোভার স্টেট। |
| **`brand-bg`** | `#C3DEFC` | ক্যানভাস বেস গ্রেডিয়েন্ট (`from-brand-bg`) — বডি/মডালের হালকা টপ-টিন্ট। |
| **`brand-surface`** | `#FFFFFF` | কার্ড, ইনপুট, মূল সারফেস ব্যাকগ্রাউন্ড। |
| `#F0F8FF` / `#E0F2FE` (arbitrary, টোকেন না) | — | সবচেয়ে হালকা icy-blue শেড — ব্যাকগ্রাউন্ড টিন্ট, ব্যাজ, `order-btn-glow`-এর idle স্টেট। |

---

## 🌙 ২. ডার্ক মোড সিস্টেম (Midnight Slate & Charcoal Navy)

সাইটে একটা পুরোপুরি কার্যকরী ডার্ক-মোড সিস্টেম আছে (`lib/store/themeStore.ts` + `app/globals.css`-এর `.dark` ওভাররাইড ব্লক, ৪০০+ লাইন)। এটা **শুধু লগইন করা ইউজার নিজে টগল করলেই** চালু হয় — গেস্ট/লগআউট অবস্থায় সবসময় লাইট মোড (কোড-কমেন্টে ইচ্ছাকৃতভাবে uncommented)।

| টোকেন | হেক্স কোড | ব্যবহার |
| :--- | :--- | :--- |
| **`brand-dark-bg`** | `#0B111E` | পেজ ব্যাকগ্রাউন্ড (dark mode)। |
| **`brand-dark-surface`** | `#131D31` | কার্ড/সারফেস। |
| **`brand-dark-card`** | `#182339` | নেস্টেড প্যানেল, `surface-muted` এর dark ইকুইভ্যালেন্ট। |
| **`brand-dark-border`** | `#22324D` | dark mode বর্ডার। |

`.dark` ক্লাস `<html>`-এ পড়লে `bg-white`, `text-ink`, `border-border-base`, `bg-surface-muted`, semantic কালার (red/emerald/amber/sky/rose ৫০-৯৫০ শেড) সহ শতাধিক ইউটিলিটি ক্লাসের জন্য সরাসরি override বসানো আছে (দেখুন `app/globals.css` লাইন ৫২৯-৯২৭)। নতুন কোনো কম্পোনেন্ট বানানোর সময় hardcoded hex রঙ ব্যবহার করলে তার জন্য `.dark .your-class` override যোগ করতে ভুলবেন না, নাহলে dark mode-এ সেটা ভাঙা দেখাবে।

> ⚠️ **পাওয়া অসামঞ্জস্য:** `app/layout.tsx`-এর ইনলাইন স্ক্রিপ্ট কুকি/localStorage না পেলে OS-এর `prefers-color-scheme: dark`-ও চেক করে থিম বসায়, অথচ `themeStore.ts`-এর কমেন্ট বলছে সিস্টেম প্রেফারেন্স ইচ্ছাকৃতভাবে চেক করা হয় না। ফলে প্রথমবার আসা কোনো গেস্টের ডিভাইস dark মোডে থাকলে সাইট না চাওয়া সত্ত্বেও dark দেখাতে পারে। ঠিক করার আগে কনফার্ম করে নেওয়া দরকার।

---

## ⚪ ৩. নিউট্রাল ও ফিডব্যাক কালার (Neutral & Semantic Palette)

| টোকেন | হেক্স কোড | ব্যবহার |
| :--- | :--- | :--- |
| **`ink`** | `#1A1A1A` | মূল টেক্সট, শিরোনাম। |
| **`muted`** | `#6B7280` | মেটা-টেক্সট, সাবটাইটেল, প্লেসহোল্ডার। |
| **`surface-muted`** | `#F3F4F6` | নিউট্রাল সফট ব্যাকগ্রাউন্ড, ডিজেবল্ড স্টেট। |
| **`border-base`** | `#E5E7EB` | হেয়ারলাইন ডিভাইডার, কার্ড বর্ডার। |
| **`success`** | `#10B981` | অর্ডার কনফার্মেশন, ইন-স্টক ব্যাজ। |
| **`gold`** | `#D4A853` | স্টার রেটিং, VIP/অফার ট্যাগ। |
| **`info`** | `#3B82F6` | ইনফো ব্যানার, প্রাইমারি CTA গ্রেডিয়েন্টের একটা প্রান্ত (`from-info to-brand-light`)। |

টোস্ট নোটিফিকেশন ও স্ট্যাটাস ব্যাজে Tailwind-এর স্ট্যান্ডার্ড `emerald/rose/amber/sky` ৫০-৯৫০ শেডও ব্যবহার হয় (দেখুন সেকশন ১২) — এগুলো উপরের কাস্টম টোকেন থেকে আলাদা কিন্তু সমান বৈধ।

---

## 🔤 ৪. টাইপোগ্রাফি ও ফন্ট সিস্টেম

- **বডি ও শিরোনাম ফন্ট:** `DM Sans` (ইংরেজি) + `Hind Siliguri` (বাংলা) — Tailwind টোকেন `font-body`।
- **🚫 নো-সেরিফ রুল:** কোনো হেডিং/টেক্সটে সেরিফ ফন্ট (`font-display` / Playfair Display) ব্যবহার করা যাবে না — সব হেডিং `font-body font-bold text-ink`।
- **🔢 ডিজিট-অনলি Noto Sans Bengali:** `app/layout.tsx`-এ Google Fonts-এর `text=` প্যারাম দিয়ে শুধু ০-৯ বাংলা ডিজিটের জন্য `unicode-range` সীমাবদ্ধ করা আছে, ফলে বাংলা/ইংরেজি মিশ্র টেক্সটে সংখ্যা সবসময় স্পষ্ট ও সমানুপাতিক দেখায়, আলাদা `<span>` ছাড়াই।

> ⚠️ **পাওয়া অসামঞ্জস্য:** `app/reset-password/ResetPasswordClient.tsx`-এর একটা হেডিং-এ `font-display` (সেরিফ) বসানো আছে — উপরের নো-সেরিফ রুলের একমাত্র ব্যতিক্রম, সম্ভবত ভুলবশত।

---

## 📱 ৫. ব্রেকপয়েন্ট (Responsive Breakpoints)

কাস্টম স্ক্রিন সেট (`tailwind.config.ts`): `xs: 359px`, `sm2: 411px`, `sm: 480px`, `md: 768px`, `lg: 1024px`, `xl: 1200px`, `2xl: 1440px`। শুরুর দুটো (`xs`, `sm2`) ছোট/মাঝারি অ্যান্ড্রয়েড ফোনের জন্য কাস্টম যোগ করা — Tailwind-এর ডিফল্ট সেটে নেই। এছাড়া `hover`/`group-hover`/`peer-hover` ভ্যারিয়েন্ট কাস্টমভাবে override করা আছে (`@media (hover: hover) and (pointer: fine)`) যাতে টাচ ডিভাইসে "স্টিকি হোভার" (ট্যাপ করলে হোভার স্টেট আটকে থাকা) বাগ না হয়।

---

## 🖼️ ৬. সারফেস ও লেআউট প্যাটার্ন

### ১. ট্রাই-কালার ব্যাকগ্রাউন্ড ক্যানভাস
- ৩০+ পেজ/মডাল/ড্রয়ারে একই গ্রেডিয়েন্ট: `bg-gradient-to-b from-brand-bg/35 via-[#DCEBFD]/45 to-white`।
- **নিয়ম:** কোনো বিচ্ছিন্ন ছোপযুক্ত ডার্ক ব্যাকগ্রাউন্ড থাকবে না (লাইট মোডে) — সারা সাইটে একই ফ্রেশ স্কাই-ব্লু থেকে সাদার ক্যানভাস।

### ২. ফ্রস্টেড গ্লাস ন্যাভবার (`navbar-glass`)
- `border border-white/70 bg-white/80 shadow-sh1 backdrop-blur-[10px] rounded-[35px]` — Navbar-এ এবং প্রায় সব `loading.tsx` স্কেলিটনে ব্যবহৃত (Suspense fallback-এও একই গ্লাস লুক ধরে রাখার জন্য)।
- Safari fallback: `-webkit-backdrop-filter` আলাদা করে বসানো আছে; backdrop-filter সাপোর্ট না থাকলে সলিড সাদা ব্যাকগ্রাউন্ডে গ্রেসফুলি ফলব্যাক করে।

### ৩. মডাল ও পপআপ হেডার ডেকোরেশন
- প্রতিটি মডালে ১৪% অপাসিটির লাইন-আর্ট জলছাপ (`HeaderDecor @ text-brand-light/[0.14]`)।
- টপ-রাইট ক্লোজ বাটন: `h-8 w-8 rounded-full border border-white/60 bg-white/80 text-ink/60`।
- হেডারের নিচে `border-b border-ink/10` হেয়ারলাইন ডিভাইডার।

---

## ⬜ ৭. বর্ডার-রেডিয়াস কনভেনশন

- **`rounded-full` ডিফল্ট:** পুরো কোডবেসে ৪৪০+ জায়গায় ব্যবহৃত — বাটন, ব্যাজ, অ্যাভাটার, ফ্লোটিং আইকনের ডিফল্ট শেপ পিল/সার্কেল।
- **কার্ড/মডাল/শিট:** নির্দিষ্ট arbitrary পিক্সেল ভ্যালু কম্পোনেন্ট-ভেদে ভিন্ন হতে পারে — সচরাচর দেখা মান: `rounded-[28px]`, `rounded-[24px]`, `rounded-[18px]`, `rounded-[14px]`, `rounded-[12px]`, `rounded-[10px]`, `rounded-[9px]` (Tailwind-এর ডিফল্ট `rounded-xl`/`rounded-lg`/`rounded-2xl`ও মাঝেমধ্যে সরাসরি ব্যবহৃত)।
- **`rounded-brand`** (`12px`, কনফিগে সংজ্ঞায়িত) হাতে-গোনা কয়েক জায়গায় ব্যবহৃত — নতুন কম্পোনেন্টে ১২px চাইলে এটাই ব্যবহার করা উচিত, নতুন arbitrary `rounded-[12px]` না বসিয়ে।

---

## 🌓 ৮. শ্যাডো টোকেন

| টোকেন | CSS ভ্যালু | ব্যবহার |
| :--- | :--- | :--- |
| `shadow-2xs` | `0 1px 2px rgba(0,0,0,.04)` | মাইক্রো এলিমেন্ট। |
| `shadow-xs` | `0 1px 3px rgba(0,0,0,.05)` | সূক্ষ্ম ডেপথ। |
| `shadow-sh1` | `0 1px 4px rgba(0,0,0,.07)` | ইনপুট, ছোট বাটন, সাবটল কার্ড (৬৩ বার ব্যবহৃত)। |
| `shadow-sh2` | `0 4px 18px rgba(0,0,0,.10)` | প্রধান কার্ড, ন্যাভবার, প্রাইমারি বাটন (৬৫ বার)। |
| `shadow-sh3` | `0 8px 36px rgba(0,0,0,.13)` | ফ্লোটিং বাটন, ড্রয়ার, সেন্ট্রাল মডাল (৩৯ বার)। |

---

## ✨ ৯. মোশন ও অ্যানিমেশন সিস্টেম

- **`transition-brand`** — `all 250ms cubic-bezier(.4,0,.2,1)`, সাইটজুড়ে ইউনিভার্সাল ট্রানজিশন টাইমিং।
- **`shimmer-sheen`** — প্রাইমারি CTA বাটনে ৬-সেকেন্ডের লুপিং আলোর বিম sweep।
- **`shimmer-sheen-oneshot`** — একবারের জন্য চলা শিমার (`.shimmer-sheen-play` ক্লাস যোগ করলে ট্রিগার হয়), যেখানে বারবার লুপ করা distracting হতো সেখানে ব্যবহৃত।
- **`card-hover-glow`** — শুধু desktop hover-এ (`@media (hover: hover) and (pointer: fine)`) কার্ড সামান্য উপরে ওঠে (`translateY(-2px)`) + সফট ব্লু গ্লো শ্যাডো।
- **`order-btn-glow`** — দেখুন সেকশন ১১.৪।
- **কীফ্রেম লাইব্রেরি (`tailwind.config.ts`):** `cart-jiggle` (কার্টে আইটেম যোগ হলে), `section-reveal` (স্ক্রল-রিভিল), `badge-hot-glow` (হট/অফার ব্যাজ পালস), `heartbeat` (উইশলিস্ট হার্ট), `ripple` (ট্যাপ ফিডব্যাক), `liquid-wobble` (নিচে দেখুন ৯.১), `soft-fade-in`।
- **গ্লোবাল reduced-motion সাপোর্ট:** `prefers-reduced-motion: reduce` হলে সব অ্যানিমেশন/ট্রানজিশন duration `0.01ms`-এ নেমে আসে — প্রতিটি নতুন কাস্টম অ্যানিমেশনেও একই ফলব্যাক যোগ করা আবশ্যক (যেমন `shimmer-sheen`, `order-btn-glow`-তে আছে)।

### ৯.১ সিগনেচার মাইক্রো-ইন্টারঅ্যাকশন
- **উইশলিস্ট ফ্লাইং-হার্ট:** প্রোডাক্ট কার্ডের হার্ট আইকনে ট্যাপ করলে একটা ছোট হার্ট arc-path-এ উড়ে গিয়ে Navbar-এর উইশলিস্ট আইকনে আঘাত করে (`requestAnimationFrame`-ভিত্তিক, `WishlistFlyOverlay.tsx`); Navbar আইকন জিগল করে ও নিচ থেকে ওপরে ব্র্যান্ড-ব্লু "তরল" দিয়ে ভরে ওঠে (`liquid-wobble` কীফ্রেম, clip-path liquid fill), কিছুক্ষণ থেমে খালি হয়ে যায়।
- **অ্যাকাউন্ট পেজ ডে/নাইট স্কাই সিন:** থিম টগলের একটা সাজানো ভিজ্যুয়াল — তারা (`twinkling`/`starShimmer`), মেঘ (`cloudDrift`), পাখি (`birdFly`), বজ্রপাত (`lightningFlash`), বৃষ্টি (`rainDropFall`), জোনাকি (`fireflyGlow`), আর সূর্য/চাঁদের হ্যালো গ্লো (`sunCoronaPulse` / `moonGlowAura`) — `app/account/AccountClient.tsx`-এ।

---

## 🖱️ ১০. স্ক্রলবার সিস্টেম

- **গ্লোবাল ডিফল্ট (পুরো সাইট):** `html` ট্যাগে `scrollbar-width: thin` + কাস্টম `::-webkit-scrollbar` — ৫px চওড়া, সিগনেচার sky-blue পিল থাম্ব (`rgba(68,167,252,.35)`, hover-এ `.70`), transparent ট্র্যাক।
- **`.sleek-scrollbar`** — একই কালার স্কিম কিন্তু আরও চিকন (৪px), ড্রয়ার/লিস্ট/মোডালের ভেতরের স্ক্রলের জন্য (কার্ট, উইশলিস্ট, অ্যাকাউন্ট পেজ, সার্চ ড্রপডাউন ইত্যাদি)।
- **`.no-scrollbar`** — সম্পূর্ণ অদৃশ্য কিন্তু স্ক্রলযোগ্য (ওভারলে, ইনভয়েস কার্ড, লগইন মোডাল, প্রোডাক্ট গ্যালারি থাম্বনেইল)।
- Dark mode-এ থাম্ব কালার আলাদাভাবে override করা আছে (`.dark ::-webkit-scrollbar-thumb`)।

---

## 🔘 ১১. প্রমিত বাটন ও কম্পোনেন্ট সাইজিং

### ১. সিগনেচার প্রাইমারি CTA বাটন
`shimmer-sheen w-full rounded-full bg-gradient-to-r from-info to-brand-light py-[13.5px] font-body text-[15px] font-bold text-white shadow-sh2 transition-[filter] duration-brand hover:brightness-[1.03] active:scale-95 disabled:opacity-60`

### ২. স্টিকি বটম বার (`ProductDetailClient.tsx`)
"অর্ডার করুন" ও "কার্ট" বাটনের উচ্চতা `h-[42px]`, `rounded-[12px]`-এ লক করা।

### ৩. ফ্লোটিং বাটন স্ট্যাক (bottom-right, সব `h-14 w-14 rounded-full bg-brand-light text-white shadow-sh3`)
- চ্যাট/মেসেঞ্জার বাটন (`FloatContactButtons.tsx`): `bottom-5 right-5`
- কার্ট ব্যাজ (`FloatCartBadge.tsx`): `bottom-[92px] right-5`
- ব্যাক-টু-টপ (`BackToTopButton.tsx`): `bottom-[164px] right-5`

### ৪. রেডিয়াল গ্লো কালার-হোভার সিস্টেম (`.order-btn-glow`)
হোমপেজ প্রোডাক্ট কার্ডের "অর্ডার করুন" বাটনে (`ProductCard.tsx`) ব্যবহৃত — hover (ডেস্কটপ) বা tap/`:active`-এ (মোবাইল) ব্যাকগ্রাউন্ডের radial-gradient পজিশন/কালার-স্টপ `@property`-ভিত্তিক কাস্টম প্রপার্টি ট্রানজিশনে smoothly শিফট হয়ে glow sweep তৈরি করে (`app/globals.css`)।
**কালার রুল:** idle বা hover কোনো অবস্থাতেই `#44A7FC`-এর চেয়ে গাঢ় নীল ব্যবহার করা যাবে না। বর্তমান প্যালেট: `#44A7FC → #C3DEFC → #E0F2FE / #F0F8FF`। টেক্সট: idle `#0284C7`, hover/active `#0062E3`। ভবিষ্যতে অন্য বাটনে একই ইফেক্ট লাগাতে হলে এই একই ক্লাস/কালার-রুল অনুসরণ করতে হবে।

---

## 🔔 ১২. টোস্ট নোটিফিকেশন সিস্টেম

`GlobalOverlays.tsx`-এর `TOAST_THEMES` — হালকা পাস্টেল ব্যাকগ্রাউন্ড + বোল্ড কালারড টেক্সট + সলিড আইকন সার্কেল:

| টাইপ | ক্লাস |
| :--- | :--- |
| success | `bg-emerald-50/95 border-emerald-300/80` |
| error | `bg-rose-50/95 border-rose-300/80` |
| info | `bg-sky-50/95 border-sky-300/80` |
| warning | `bg-amber-50/95 border-amber-300/80` |

**নিয়ম:** টোস্টের টাইপ কখনো মেসেজ-টেক্সট কীওয়ার্ড sniff করে অনুমান করা যাবে না (আগে একবার এভাবে একটা error টোস্ট ভুলে সবুজ/success রঙে দেখাচ্ছিল) — কল-সাইটে সবসময় explicit `'success' | 'error' | 'warning' | 'info'` টাইপ পাস করতে হবে।

---

## 🎨 ১৩. আইকন সিস্টেম ও নো-ইমোজি পলিসি

1. **🚫 নো-ইমোজি পলিসি:** UI-তে কাঁচা ইমোজি নিষিদ্ধ — মিনিমাল, প্রফেশনাল হ্যান্ড-ড্রন লাইন SVG আইকন ব্যবহার করতে হবে। *(ব্যতিক্রম: শুধু `OrderCard`-এর মেটা-ইনফো লাইনে 📅 ও 👤)*।
2. **আইকন কালার স্ট্যান্ডার্ড:** আইকন/ব্যাজ/হাইলাইটে কখনো গাঢ় নীল নয় — সবসময় `text-brand-light` / `bg-brand-light` (`#44A7FC`)।
3. **ক্যাটাগরি আইকন:** `lib/categoryData.ts`-এর প্রতিটি ক্যাটাগরির নিজস্ব কাস্টম ভেক্টর SVG (ইউনিক `vcx_` gradient ID প্রিফিক্স, ID কলিশন এড়াতে)।

---

## 💰 ১৪. প্রাইস ও ইউনিট ডিসপ্লে কনভেনশন

1. **প্রাইস রেন্ডারিং:** `৳{price.toLocaleString('en-US')}` → যেমন **৳1,250**, **৳11,350**।
2. **পিস লেবেল:** বাংলায় **"৩ পিছ"**, ইংরেজিতে **"3 Pcs"**।
3. **ডেলিভারি চার্জ:** ঢাকা সিটি **৳70**, ঢাকার বাইরে সারা বাংলাদেশ **৳120**।

---

## 🩹 ১৫. অডিটে পাওয়া অসামঞ্জস্য (ফ্ল্যাগড, এখনো ফিক্স করা হয়নি)

এই আইটেমগুলো ডিজাইন-সিস্টেম রুলের সাথে না মেলা বা সম্ভাব্য বাগ হিসেবে পাওয়া গেছে — রেফারেন্সের জন্য এখানে রাখা হলো, কোনোটাই এই অডিটে কোড পরিবর্তন করে ফিক্স করা হয়নি:
- `ResetPasswordClient.tsx`-এ একমাত্র `font-display` (সেরিফ) ব্যবহার — নো-সেরিফ রুল ভঙ্গ করছে।
- Dark mode-এর OS `prefers-color-scheme` ফলব্যাক বনাম "শুধু ম্যানুয়াল টগল" কমেন্টের মধ্যে সাংঘর্ষিকতা (সেকশন ২)।
- `lib/scrollLock.ts` ও `lib/bodyScrollLock.ts` — দুটো প্রায়-ডুপ্লিকেট বডি-স্ক্রল-লক মডিউল, ভিন্ন ভিন্ন কম্পোনেন্ট ব্যবহার করছে, একত্রীকরণ বাকি।
